<?xml version="1.0" encoding="utf-8"?>
<!-- Transparent 1x1 drawable — used as notification small icon when the
     user wants to hide the status bar icon. The notification still appears
     in the notification shade at IMPORTANCE_LOW (not minimized), but the
     status bar shows nothing visible. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="1dp"
    android:height="1dp"
    android:viewportWidth="1"
    android:viewportHeight="1">
    <path
        android:fillColor="#00000000"
        android:pathData="M0,0 L1,0 L1,1 L0,1 Z" />
</vector>
